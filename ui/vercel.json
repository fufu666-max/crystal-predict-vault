{
  "version": 2,
  "buildCommand": "cd .. && npm install && npm run compile && npm run typechain && cd ui && npm run build",
  "outputDirectory": "dist",
  "installCommand": "cd .. && npm install && cd ui && npm install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

